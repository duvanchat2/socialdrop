---
phase: 02-ai-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env
  - apps/api/src/modules/assistant/assistant.module.ts
  - apps/api/src/modules/assistant/assistant.controller.ts
  - apps/api/src/modules/assistant/assistant.service.ts
  - apps/api/src/modules/assistant/dto/chat.dto.ts
  - apps/api/src/app/app.module.ts
autonomous: true
---

<objective>
## Goal
Install @anthropic-ai/sdk, configure ANTHROPIC_API_KEY, and create a NestJS assistant module with a POST /api/assistant/chat endpoint.

## Purpose
Provides the AI backend for SocialDrop's content assistant. Claude API generates post captions and social media content suggestions.

## Output
- @anthropic-ai/sdk installed in package.json
- ANTHROPIC_API_KEY added to .env
- apps/api/src/modules/assistant/ (module, controller, service, DTO)
- AssistantModule registered in AppModule
</objective>

<context>
@.paul/PROJECT.md
@apps/api/src/app/app.module.ts
@apps/api/src/main.ts
</context>

<acceptance_criteria>

## AC-1: SDK installed
Given the project dependencies
When package.json is checked
Then @anthropic-ai/sdk appears in dependencies

## AC-2: Env var configured
Given the .env file
When ANTHROPIC_API_KEY is checked
Then the key exists (value can be placeholder)

## AC-3: Chat endpoint exists
Given the NestJS API is running
When POST /api/assistant/chat is called with { "message": "Help me write a post" }
Then a 200 response is returned with { "reply": "..." }

## AC-4: Module registered
Given AppModule
When AssistantModule is imported
Then NestJS bootstraps without errors

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk and add ANTHROPIC_API_KEY to .env</name>
  <files>package.json, .env</files>
  <action>
    Run: npm install @anthropic-ai/sdk
    Then append to .env: ANTHROPIC_API_KEY=your-key-here
    Do NOT remove any existing .env vars.
  </action>
  <verify>package.json dependencies includes @anthropic-ai/sdk; .env contains ANTHROPIC_API_KEY line</verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Create AssistantModule (controller + service + DTO)</name>
  <files>
    apps/api/src/modules/assistant/dto/chat.dto.ts,
    apps/api/src/modules/assistant/assistant.service.ts,
    apps/api/src/modules/assistant/assistant.controller.ts,
    apps/api/src/modules/assistant/assistant.module.ts
  </files>
  <action>
    Create chat.dto.ts with ChatDto { message: string } using @IsString() @IsNotEmpty() from class-validator.
    Create assistant.service.ts: inject ConfigService, use Anthropic client from @anthropic-ai/sdk, expose async chat(message: string): Promise<string> that calls messages.create with model claude-3-5-haiku-20241022, max_tokens 1024.
    Create assistant.controller.ts: @ApiTags('assistant') @ApiOperation, POST /chat endpoint accepting ChatDto, returns { reply: string }.
    Create assistant.module.ts: exports AssistantService, imports ConfigModule.
  </action>
  <verify>Files exist with no TypeScript errors (npx nx build api --skip-nx-cache)</verify>
  <done>AC-3 and AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Register AssistantModule in AppModule</name>
  <files>apps/api/src/app/app.module.ts</files>
  <action>
    Import AssistantModule from '../modules/assistant/assistant.module.js'
    Add AssistantModule to the imports array in @Module decorator.
  </action>
  <verify>npx nx build api --skip-nx-cache exits 0</verify>
  <done>AC-4 satisfied: NestJS bootstraps with AssistantModule registered</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- libs/prisma/prisma/schema.prisma
- docker-compose.yml
- Any existing .env vars (only ADD ANTHROPIC_API_KEY)
- Other existing modules

## SCOPE LIMITS
- No authentication on the assistant endpoint for now
- No streaming responses (single response only)
- Backend only — frontend is plan 02-02

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm install @anthropic-ai/sdk succeeded
- [ ] .env has ANTHROPIC_API_KEY
- [ ] npx nx build api --skip-nx-cache exits 0
- [ ] No TypeScript errors
- [ ] All AC met
</verification>

<success_criteria>
- @anthropic-ai/sdk in dependencies
- ANTHROPIC_API_KEY in .env
- POST /api/assistant/chat endpoint exists and compiles
- AssistantModule in AppModule
- Zero TS errors
</success_criteria>

<output>
After completion, create .paul/phases/02-ai-assistant/02-01-SUMMARY.md
</output>
