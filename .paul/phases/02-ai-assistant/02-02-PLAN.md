---
phase: 02-ai-assistant
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/assistant/page.tsx
  - apps/web/src/components/AppShell.tsx
autonomous: true
---

<objective>
## Goal
Create the Next.js assistant chat page and add "Asistente IA" to the sidebar navigation.

## Purpose
Gives users a frontend interface to chat with the Claude-powered assistant from SocialDrop's dashboard.

## Output
- apps/web/src/app/assistant/page.tsx — Chat UI calling POST /api/assistant/chat
- apps/web/src/components/AppShell.tsx — New sidebar nav entry "Asistente IA"
</objective>

<context>
@.paul/PROJECT.md
@apps/web/src/components/AppShell.tsx
@apps/web/src/lib/api.ts
</context>

<acceptance_criteria>

## AC-1: Chat page renders
Given the Next.js app
When the user navigates to /assistant
Then a chat UI with an input field and message history is shown

## AC-2: Chat sends message to API
Given the chat page
When the user types a message and submits
Then POST /api/assistant/chat is called and the reply appears in the conversation

## AC-3: Sidebar link exists
Given any page in the app
When the sidebar is visible
Then "Asistente IA" link is present and navigates to /assistant

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create assistant chat page</name>
  <files>apps/web/src/app/assistant/page.tsx</files>
  <action>
    Create 'use client' page component.
    State: messages array { role: 'user'|'assistant', text: string }[], input string, loading boolean.
    UI: scrollable message list, text input, send button.
    On submit: POST to apiFetch('/api/assistant/chat', { method:'POST', body: JSON.stringify({ message: input }) }) then append reply to messages.
    Style: dark theme consistent with other pages (bg-gray-900, text-white, indigo accent).
    No SSR issues — this is fully client-side.
    Page title: "Asistente IA".
  </action>
  <verify>Page renders at /assistant, input submits to POST /api/assistant/chat</verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Add "Asistente IA" to AppShell sidebar</name>
  <files>apps/web/src/components/AppShell.tsx</files>
  <action>
    Import BotMessageSquare (or MessageSquare) from lucide-react for the icon.
    Add to NAV array: { href: '/assistant', label: 'Asistente IA', icon: BotMessageSquare }
    Place it after "Integraciones" entry or at end of nav list.
  </action>
  <verify>Sidebar shows "Asistente IA" link; clicking navigates to /assistant</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- apps/web/src/lib/api.ts
- Other existing pages
- AppShell structure (only add to NAV array)
- libs/prisma/*, docker-compose.yml, nginx configs

## SCOPE LIMITS
- No auth required for assistant page
- No streaming UI (show spinner during fetch, then full reply)
- No markdown rendering in chat (plain text only)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] apps/web/src/app/assistant/page.tsx created
- [ ] AppShell.tsx NAV has Asistente IA entry
- [ ] npx nx build web --skip-nx-cache exits 0
- [ ] No TypeScript errors
- [ ] All AC met
</verification>

<success_criteria>
- /assistant page exists and compiles
- Sidebar shows Asistente IA
- Zero TS errors
</success_criteria>

<output>
After completion, create .paul/phases/02-ai-assistant/02-02-SUMMARY.md
</output>
